syntax = "proto2";

message InputNode {
  map<string, uint64> links = 1;
  optional bytes value = 2;
}

message OutputNode {
  map<string, uint64> clock = 1;
  required string key = 2;
  required uint64 seq = 3;
  optional InputNode node = 4;
  optional bytes value = 5;
}
