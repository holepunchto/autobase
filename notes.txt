corestore parent gots to go, thats internal and breaks stuff

resumeSession thing in moveto doesnt look right, prop needs a clone thing

corruption helper:

// const roots = await core.state.tree.getRoots(indexedLength)
// const flat = require('flat-tree')

// for (const root of roots) {
//   let [l, r] = flat.spans(root.index)
//   for (; l <= r; l++) {
//     console.log(l, !!(await core.state.tree.get(l, true, null, true)))
//   }
// }

// for (let i = 0; i < indexedLength; i++) {
//   console.log(i, await core.get(i))
// }

dbl check extension, should prop move off cores to a protomux for simplicity btw

dbl ack stuff again with chris, especially the should write stuff
